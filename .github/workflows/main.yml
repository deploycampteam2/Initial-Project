name: Test Self-Hosted Runner

on: [push]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      
      - name: Test runner
        run: |
          echo "Hello from VPS runner!"
          echo "Current directory: $(pwd)"
          echo "Current user: $(whoami)"
          ls -la
